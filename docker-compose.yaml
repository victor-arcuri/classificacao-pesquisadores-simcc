services:
  # database:
  #   image: pgvector/pgvector:pg17
  #   restart: always
  #   environment:
  #     POSTGRES_PASSWORD: ${DB_SENHA}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #     - ./database/init_scripts:/docker-entrypoint-initdb.d
  #   ports: 
  #     - "${DB_PORT_HOST}:${DB_PORT_CONTAINER}"
  #   container_name: ${NOME_CONTAINER_DB}

  curriculo_db:
    restart: always
    build:  
      context: ./database_sec
      dockerfile: curriculo.dockerfile
    environment:
      POSTGRES_USER: ${CURRICULO_DB_USER}
      POSTGRES_PASSWORD: ${CURRICULO_DB_PASSWORD}
      POSTGRES_DB: ${CURRICULO_DB_NAME}
    volumes:
      - curriculo_pgdata:/var/lib/postgresql/data
    ports: 
      - "${CURRICULO_DB_PORT_HOST}:${DB_PORT_CONTAINER}"
    container_name: ${NOME_CONTAINER_CURRICULO_DB}

  # front-end:
  #   depends_on: 
  #     api:
  #       condition: service_started
  #   build:
  #     context: ./classificacao-front
  #     dockerfile: front.dockerfile
  #   image: ${NOME_IMAGEM_FRONT}
  #   container_name: ${NOME_CONTAINER_FRONT}
  #   ports:
  #     - "${PORT_HOST_FRONT}:${PORT_CONTAINER_FRONT}"
  #   volumes:
  #     - ./classificacao-front:/app
  #     - /app/node_modules
  #   command: npm run dev

  # api:
  #   depends_on: 
  #     database:
  #       condition: service_started
  #   build: 
  #     context: ./api
  #     dockerfile: api.dockerfile
  #   image: ${NOME_IMAGEM_API}
  #   container_name: ${NOME_CONTAINER_API}
  #   ports:
  #     - "${PORT_HOST_API}:${PORT_CONTAINER_API}"
  #   volumes:
  #     - ./api:/app
  #   environment:
  #     FRONT_URL: "http://localhost:${PORT_HOST_FRONT}"
  #     API_PORT: ${PORT_CONTAINER_API}
  #     DATABASE_URL: "postgresql://${DB_USERNAME}:${DB_SENHA}@database:${DB_PORT_CONTAINER}/${DB_NAME}"
  #   command: sh -c "npx prisma generate && npm run dev"

volumes:
  # pgdata:
  curriculo_pgdata:
